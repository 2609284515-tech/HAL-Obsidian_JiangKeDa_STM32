需要[[GPIO初始化]]
```
if (GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_11) == 0)
if (GPIO_ReadOutputDataBit(GPIOA, GPIO_Pin_2) == 0)
```

# HAL库
HAL库直接删了读odr的函数，剩下这个读idr的，因为idr也可以反映输出
```
if (HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_3) == 0)
```