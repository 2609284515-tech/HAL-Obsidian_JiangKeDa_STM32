需要[[配置时基单元]]
这个代码要永远放在[[Init函数]]的最后执行
```
/*启动定时器*/
	TIM_Cmd(TIM2, ENABLE);
```

# HAL库

```
extern TIM_HandleTypeDef htim2;
```

```
void Timer_Init(void)
{
    HAL_TIM_Base_Start_IT(&htim2);
}
```

## PWM

```
void PWM_Init(void)
{
    HAL_TIM_PWM_Start(&htim2, TIM_CHANNEL_1);
}
```