放到System组
```
Delay_us(uint32_t us);
Delay_ms(uint32_t ms);
Delay_s(uint32_t s);
```

# HAL库
这个函数依赖systick中断，systick中断的优先级是最低的，如果在中断函数里使用会卡死，复刻的话还是要用江科大库
```
HAL_Delay(1000);
```

把型号的include改成main的include
头文件
```
#include <stdint.h>
```